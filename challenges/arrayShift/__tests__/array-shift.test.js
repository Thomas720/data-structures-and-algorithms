'use strict';

const arrayShift = require('../array-shift');

describe('Test Array-Shift', () => {
    it('It should add an element in the middle of the current array', () => {
        expect([1,2,3,4,9,5,6,7,8]).toBeTruthy();
    })
})